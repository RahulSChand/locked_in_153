# Add your own OpenAI credentials to enable dynamically generated interventions.
# Leave both values empty to use deterministic local fallback decisions.
OPENAI_API_KEY=
OPENAI_MODEL=
AGENT_SERVER_PORT=8787
